import { AuthForm } from "@/components/auth/AuthForm"

const page = () => {
    return (

        <div>
            original login
            <AuthForm type='login' />
        </div>

    )
}

export default page